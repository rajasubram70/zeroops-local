// ─── ROLES CONFIGURATION ─────────────────────────────────────────────────────
// Source of truth is now: src/data/customer/ericsson/roles.json
// This file re-exports from the loader so the rest of the app is unchanged.

import { ROLES, NAV_ITEMS } from '../data/customer/loader.js';

export { ROLES, NAV_ITEMS };
